# 项目协作约定

- 总是用中文回复用户；代码标识符按 Python 惯例使用英文。
- 课程设计总周期为 14 周，以 `03-实施方案/课程设计完整实施方案.md` 为当前实施基线。
- 技术路线已冻结；当前里程碑为 `M1 — Simulation Reliability Baseline`。事件、约束、指标和随机流以 `docs/simulation-contract.md` 为准；M1 未通过前不得启用 CMA-ES。PPO、GNN、显式 AMHS、设施布局优化和三维数字孪生不进入主线。
- SMT2020 原始数据位于根目录 `datasets/` 并随仓库克隆；已纳入版本控制的数据保持内容只读、原始字节不可变，不提交额外未授权数据。派生数据写入 `runs/` 或 `artifacts/`。原始教学资料和分组信息不提交公开仓库。
- 源代码位于 `04-项目实现`。初始化脚手架不代表仿真或算法已实现；文档、界面和报告均需区分计划、已实现和已验证。
- 当前物流模型使用外生随机搬运延迟，不以设施布局规划为前置任务。可视化坐标与物流计算解耦；示意布局不能宣称真实布局。
- 二维动态展示采用同一运行的事件和状态，不在前端独立产生影响业务指标的随机过程。
- 明确且不需要创造性判断的脚手架、格式整理、重复性实现与检查，可以委派给 GPT-5.6 luna；委派需限定文件范围，主代理复核结果。涉及模型假设、研究设计、指标口径的工作由主代理判断。
- 使用 `main` 和短期 `feature/<topic>` 分支，通过 PR 审阅合并。不要提交凭据、个人配置、额外未授权数据、虚拟环境或大量实验输出。
- 不预设性能提升数字；所有实验结论必须关联数据版本、场景配置、随机种子和运行产物。
